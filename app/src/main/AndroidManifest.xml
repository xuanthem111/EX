<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myDemo">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyDemo"
        tools:ignore="AllowBackup">

        <activity android:name=".mvvm.view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvvm.view.SignUp"/>
        <activity android:name=".home.Target"/>
        <activity android:name=".home.Chat" />
        <activity android:name=".home.Profile" />
        <activity android:name=".home.Achievement" />
        <activity android:name=".mvvm.ForgotPassword" />

    </application>

</manifest>